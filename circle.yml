machine:
  node:
    version: 6

dependencies:
  pre:
    - npm config set "//registry.npmjs.org/:_authToken" $NPM_AUTH
  override:
    - make node_modules

test:
  override:
    - make test

deployment:
  publish:
    tag: /v?[0-9]+(\.[0-9]+)*(-.+)?/
    commands:
      - npm publish
